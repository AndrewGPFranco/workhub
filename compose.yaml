services:
  postgres:
    image: 'postgres:16-alpine'
    container_name: db_workhub
    environment:
      - 'POSTGRES_DB=${DB}'
      - 'POSTGRES_PASSWORD=${PASSWORD_DB}'
      - 'POSTGRES_USER=${USER_DB}'
    ports:
      - '5432:5432'